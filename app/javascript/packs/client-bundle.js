import ReactOnRails from 'react-on-rails';

import RoomIndex from "../components/rooms/RoomIndex";

// Any JSX files included here will be loaded into the user's browser for display on pages using react_component
ReactOnRails.register({
  RoomIndex
});
